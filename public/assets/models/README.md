Three.js character models live here.

Current version renders lightweight procedural 3D heroes in the Mini App, so Telegram loads fast.
Drop future .glb files here and map them from the hero visual config when the designs are ready.
